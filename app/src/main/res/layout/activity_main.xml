<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:clipChildren="false"
    android:keepScreenOn="true"
    tools:context=".ui.MainActivity">

    <!-- INFO BUTTON -->
    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/btn_info"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:background="@color/transparent"
        android:contentDescription="@string/info_button_content_description"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_info"
        android:tint="@color/text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- CLOCK CONTAINER -->
    <FrameLayout
        android:id="@+id/clockContainer"
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.35">

        <com.example.mamn01_g2.ui.ClockView
            android:id="@+id/clock_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/ic_clock_bg"
            app:clockBackgroundColor="@color/orange"
            app:clockProgressColor="@color/progress"
            app:clockStrokeWidth="@dimen/clock_progress_stroke"
            app:clockTextColor="@color/text" />

        <com.example.mamn01_g2.ui.TimePicker
            android:id="@+id/time_picker"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <!-- INSTRUCTIONS -->
    <TextView
        android:id="@+id/tv_instruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:fontFamily="@font/rubik"
        android:text="@string/main_instructions"
        android:textAlignment="center"
        android:textColor="@color/text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/clockContainer" />

    <!-- RESTART BUBBLE -->
    <include
        layout="@layout/layout_restart_bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toTopOf="@+id/btn_lock"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- SNOOZE BUTTON (Secondary Style) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_snooze"
        style="@style/Widget.Mamn01.Button.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="@string/_5_min"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@+id/btn_lock"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- PRIMARY ACTION BUTTON (Visible in IDLE) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_lock"
        style="@style/Widget.Mamn01.Button.Primary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:text="@string/lock_time"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- SECONDARY ACTION BUTTON (Visible in LOCKED) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_unlock"
        style="@style/Widget.Mamn01.Button.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:text="UNLOCK"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>